# SkyVault Production Configuration
# Copy this file to .env and update the values

# ===========================================
# GitHub Configuration (for Docker image)
# ===========================================
# Version/tag of the Docker image to use
VERSION=v1.0.0

# ===========================================
# Server Configuration
# ===========================================
# Server paths (Docker internal - do not change these)
SERVER__PATH=/app
SERVER__DATA_DIR=/app/data
SERVER__MIGRATIONS_DIR=/app/migrations
SERVER__ADDR=0.0.0.0:8090

# Port to expose the application on your host machine
SERVER__PORT=8090

# ===========================================
# Database Configuration
# ===========================================
# Database name
DB__NAME=skyvault

# Database user
DB__USER=skyvault

# Database password - CHANGE THIS TO A STRONG PASSWORD!
DB__PASS=change-this-to-a-strong-password

# Database host (use 'db' for Docker deployment - this is the service name)
DB__HOST__NAME=db

# Database port (internal container port)
DB__HOST__PORT=5432

# Connection timeout in seconds
DB__CONN_TIMEOUT_SEC=30

# ===========================================
# Authentication Configuration
# ===========================================
# JWT secret key - MUST BE AT LEAST 32 CHARACTERS!
# Generate a secure key using: openssl rand -base64 32
AUTH__JWT__KEY=change-this-to-a-secure-random-key-min-32-chars

# JWT token expiration time in minutes (43200 = 30 days)
AUTH__JWT__TOKEN_TIMEOUT_MIN=43200

# ===========================================
# Media/Storage Configuration
# ===========================================
# Maximum upload size in MB (10240 = 10GB)
MEDIA__MAX_UPLOAD_SIZE_MB=10240

# Maximum direct upload size in MB before chunking is required (5000 = 5GB)
MEDIA__MAX_DIRECT_UPLOAD_SIZE_MB=5000

# Maximum chunk size in MB (100 = 100MB)
MEDIA__MAX_CHUNK_SIZE_MB=100

# ===========================================
# Logging Configuration
# ===========================================
# Log level: debug, info, warn, error
LOG__LEVEL=info
